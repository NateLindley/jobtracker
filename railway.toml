[build]
builder = "nixpacks"
buildCommand = "cd backend && rm -rf node_modules && npm install --include=dev && ./node_modules/.bin/tsc"

[deploy]
startCommand = "cd backend && node dist/src/index.js"
restartPolicyType = "ON_FAILURE"