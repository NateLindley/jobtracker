[build]
builder = "nixpacks"
buildCommand = "rm -rf node_modules && npm install --include=dev && ./node_modules/.bin/tsc"

[deploy]
startCommand = "node dist/src/index.js"
restartPolicyType = "ON_FAILURE"